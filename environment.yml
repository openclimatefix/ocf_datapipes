name: datapipes
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pip
  - pytorch>=1.13
  - rioxarray
  - torchdata>=0.5
  - torchvision
  - xarray
  - fsspec
  - zarr
  - cartopy
  - dask
  - pyproj
  - pyresample
  - geopandas
  - h5netcdf
  - scipy
  - pip:
      - einops
      - pathy
      - git+https://github.com/SheffieldSolar/PV_Live-API
      - pyaml_env
      - nowcasting_datamodel
      - gitpython
      - tqdm
      - bottleneck
